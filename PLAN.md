# MathCanvas AI 활동지 보완 계획

계획 상태: Ask Plan 확정 — committed handoff ready
기준 감사: `BENCHMARK_AUDIT_2026-07-29.md`
현재 기준 커밋: `164d77bdd709484cc523ee5a35938be51dc2a73b`

## Confirmed Decisions

### 제품과 사용자

- 주 사용자는 일반 초등 교사이며, 학생은 교사가 배포한 MathCanvas 활동을 사용한다.
- 교사는 Codex 또는 Claude Code와 대화만으로 추천과 생성을 끝낸다.
- Chrome 확장 프로그램과 Computer Use는 사용하지 않는다.
- macOS와 Windows 데스크톱을 지원하고 모바일은 첫 범위에서 제외한다.
- MathCanvas 로그인은 교사가 전용 Chrome에서 직접 처리한다.
- AI는 항상 새 캔버스만 만들고 기존 캔버스는 수정·삭제하지 않는다.
- 생성 성공 뒤에는 같은 전용 Chrome의 MathCanvas 편집 화면을 보여 준다.
- 제작자 개인 파일럿을 통과한 뒤 여러 교사에게 확대한다.

### 첫 학습 활동

- 첫 검증 활동은 초등 5학년 권장 `[6수01-07] 분모가 다른 분수의 크기를 비교하고 그 방법을 설명할 수 있다.`이다.
- 두 분수 띠를 같은 전체와 같은 출발선에 놓아 길이를 비교하는 경험이 핵심이다.
- 학생은 숫자만 비교하지 않고 직접 조작해 어느 분수가 더 큰지 시각적으로 이해해야 한다.
- 문제 수와 난이도는 교사가 정할 수 있고 AI가 기본값을 추천한다.
- 제작 도구는 학생의 정답·오답을 판정하거나 교사의 확인 흐름을 운영하지 않는다.
- 학생이 비교 이유를 적을 수 있는 문장 입력 영역은 만들되 입력 내용을 수집·평가하지 않는다.
- 공식 2022 개정 교육과정이 기준이며 `DECK6/korean-elementary-learning-map`은 고정 커밋의 보조 자료로만 사용한다.

### 기술·안전

- 로컬 stdio MCP 서버와 별도 영구 Chrome 프로필 구조를 유지한다.
- 로그인 토큰은 MathCanvas 페이지 컨텍스트 밖으로 반환하거나 저장하지 않는다.
- 교사가 본 추천안과 생성 payload의 해시가 다르면 중단한다.
- 허용된 외부 쓰기는 새 프로젝트 생성뿐이다.
- 현재 74개 자동 테스트와 fail-closed 계약 검사를 보존한다.

### 이번 보완의 필수 목표

- 인증된 MathCanvas 계정에서 실제 생성·렌더·드래그를 끝까지 검증한다.
- 학생 화면은 `집중형 비교 실험실` 방향으로 다시 설계한다.
- 한 화면에는 현재 한 문제와 현재 행동만 크게 보여 준다.
- 주황색·하늘색 분수 띠, 낮은 정보 밀도, 큰 글자와 큰 놓기 영역을 사용한다.
- 학생은 기호를 놓은 뒤 비교 이유를 문장 틀 한 줄에 입력한다.
- 문제 전환이 없으므로 요청한 문제 수만큼 한 문제짜리 새 캔버스를 만들고 `1/N~N/N` 세트로 관리한다.
- 의미 모델의 `accepts`가 실제 MathCanvas 행동이 아니면 이름과 검증에서 제거해 지원 기능처럼 보이지 않게 한다.
- 띠의 임의 크기 변경·회전·분할 가능 여부를 실서비스에서 확인하고 과제를 깨뜨리는 조작은 막는다.
- 실제 렌더 기준 1280×720과 1024×768 화면 QA를 통과한다.
- 생성 성공 뒤 편집 화면을 열고 학생 미리보기에서 생성물의 렌더·드래그·입력을 QA한다.
- 학생 링크 자동 발행은 이번 보완 릴리스의 완료 조건에 넣지 않는다.
- 보완 착수 시 교사가 전용 Chrome에 로그인해 실제 생성과 학생 모드 검증에 참여한다.
- 사용자가 실제 생성물을 보고 품질을 승인하기 전에는 다교사 배포 완료로 표시하지 않는다.

### Selected Visual Direction

- 이름: `집중형 비교 실험실`
- 참고 방향: Amplify Classroom의 한 화면 한 과제 흐름과 Math Learning Center·Polypad의 큰 분수 조작물을 참고하되 화면이나 자산을 복제하지 않는다.
- 색: 첫째 분수는 주황, 둘째 분수는 하늘색, 공통 출발선은 빨강, 배경과 비교판은 흰색·옅은 하늘색의 중립 색을 사용한다.
- 글자: 문제의 분수와 현재 행동을 가장 크게 두고, 1280×720과 1024×768 실제 렌더에서 학생용 본문 18px 이상을 유지한다.
- 배치: 기준 학생 화면은 16:10의 한 문제 Stage로 잡고, 큰 문제·현재 조작판·한 줄 지시·현재 선택만 보이게 한다.
- 조작물: 분수 띠와 출발선이 화면의 주인공이어야 하며, 식의 색과 해당 띠의 색을 직접 연결한다.
- 움직임: MathCanvas의 실제 드래그를 사용한다. 스냅이 실제로 지원될 때만 연결하고, 지원되지 않으면 있는 기능처럼 표시하지 않는다.
- 피해야 할 것: 여러 문제 동시 노출, 18px 미만 학생 글자, 긴 설명, 숫자와 띠의 색 연결 누락, 자유 크기 변경·회전, 학생 판단을 가리는 도구 막대.

## Assumptions

- 첫 보완 릴리스는 분수 비교 템플릿의 품질을 먼저 완성하고 1~6학년 전체 템플릿 확장은 뒤로 미룬다.
- 기본 사용은 학생 1명당 기기 1대다.
- 문제 전환이 없으므로 4문제를 요청하면 한 문제짜리 새 캔버스 4개를 만든다.
- 세트 생성 중 일부가 실패하면 이미 성공한 캔버스를 다시 만들지 않고 빠진 캔버스만 이어서 만든다.
- 생성 완료 뒤 첫 번째 캔버스 편집 화면을 열고, 대화 응답에는 세트의 모든 편집 URL과 순서를 반환한다.
- 다교사 설치 패키지와 자동 업데이트는 사용자가 실제 생성물 품질을 승인한 뒤 만든다.
- 학생 개인정보와 풀이 로그를 별도 서버로 보내는 기능은 이번 범위에 넣지 않는다.

## Open Questions

- 현재 없음. 실제 MathCanvas 계약 탐색에서 새 제약이 발견되면 구현을 임의로 우회하지 않고 계획을 다시 연다.

## Non-Goals

- Chrome 확장 프로그램 재도입
- Computer Use로 화면을 대신 클릭하는 일반 자동화
- 기존 MathCanvas 프로젝트 수정·삭제
- 로그인·CAPTCHA·2단계 인증 자동 처리
- 검증되지 않은 수학 주제의 자유 생성
- 학생 정답·오답 판정과 자동 피드백
- 학생 응답 수집·분석과 교사 대시보드
- 자기 확인·교사 확인 운영 흐름
- 학생 링크 자동 발행
- 임의의 교사 수·학생 수를 성공 기준으로 삼는 파일럿
- 사용자가 실제 생성물 품질을 승인하기 전 공개 배포

## Success Definition

점수 자체를 성공으로 보지 않는다. 아래 생성물 기준을 모두 통과하고 사용자가 실제 결과를 승인해야 성공이다.

### 대화와 생성

- 교사가 학년·문제 수·난이도를 말하면 AI가 지원 범위 안에서 추천한다.
- 교사가 승인하기 전에는 MathCanvas에 쓰지 않는다.
- `4문제`를 승인하면 정확히 4개의 한 문제짜리 새 캔버스를 만든다.
- 프로젝트 제목에 동일한 세트명과 `1/4~4/4` 순서를 표시한다.
- 첫 번째 편집 화면을 열고 대화 응답에는 전체 편집 URL과 문제 순서를 반환한다.
- 중간 실패 뒤 재시도해도 이미 성공한 캔버스를 중복 생성하지 않는다.

### 수학과 교육과정

- 모든 문제는 `[6수01-07]`과 5~6학년군 범위에 맞는다.
- 모든 비교는 분모가 서로 다르고 크기가 같지 않다.
- 비교 기호, 통분식, 교사용 정답이 수학적으로 정확하다.
- 두 분수 띠는 같은 전체 폭을 사용한다.
- 쉬움·보통·어려움의 시각 차이 범위를 문제 은행 전체에서 통과한다.

### 실제 캔버스 결과

- 한 캔버스에는 한 문제만 보인다.
- 문제의 두 분수, 두 분수 띠, 공통 출발선, 기호 놓기, 문장 입력 영역이 모두 보인다.
- 식과 해당 띠는 같은 색으로 직접 연결된다.
- `출발선`이라는 말이 실제 화면에 보인다.
- 1280×720과 1024×768 실제 MathCanvas 렌더에서 글자 잘림·요소 겹침이 0개다.
- 학생용 본문은 실제 렌더 18px 이상, 드래그·입력 대상은 44×44px 이상이다.
- 분수 띠는 옮길 수 있지만 과제 중 임의로 크기·회전·분할이 바뀌지 않는다.
- `<`, `>` 기호는 옮길 수 있고 문장 입력 영역은 실제 활동 화면에서 편집할 수 있다.
- 스냅이 지원되지 않으면 스냅·정답 판정이 있는 것처럼 문서나 응답에 표시하지 않는다.

### 안전과 증거

- 기존 프로젝트 수정·삭제·덮어쓰기가 0건이다.
- 토큰·쿠키·Authorization 헤더가 MCP 응답과 로컬 작업 파일에 0건이다.
- 현재 작업본과 깨끗한 GitHub 복제본에서 타입 검사·전체 테스트·빌드를 통과한다.
- 실제 생성된 모든 캔버스의 편집 화면과 학생 미리보기 캡처를 사용자에게 보여 준다.
- 위 체크리스트를 코드 검사만으로 통과 처리하지 않고 실제 MathCanvas 화면에서 확인한다.

## Architecture Backbone

### Data Model

#### `ActivitySetSpec` — core

- `setId`
- `title`
- `grade`
- `standardCode`
- `problemCount`
- `difficulty`
- `manipulation`
- `problems[]`
- `seed`
- `templateId`
- `templateVersion`
- `setHash`

한 번의 교사 승인과 한 세트의 문제·순서를 대표하는 단일 기준이다.

#### `CanvasActivitySpec` — core

- `setId`
- `canvasIndex`
- `canvasCount`
- `problem`
- `layout`
- `fixedObjects`
- `movableObjects`
- `inputObjects`
- `canvasHash`

`ActivitySetSpec.problems[]`의 문제 하나만 포함한다. 한 캔버스에 두 번째 문제가 들어가면 schema와 validator에서 실패시킨다.

#### `CompiledCanvasProject` — core

- `canvasHash`
- `payloadHash`
- `projectTitle`
- `payload`
- `contractVersion`

MathCanvas에 보낼 한 프로젝트의 검증된 결과다.

#### `CreationBatch` — core

- `batchId`
- `setHash`
- `status`
- `items[]`
- 각 item의 `canvasIndex`, `payloadHash`, `status`, `projectId`, `editorUrl`, `errorCode`

여러 새 캔버스 생성 중 일부만 성공해도 상태를 보존하고 빠진 항목만 재개한다.

#### `RenderEvidence` — core

- `projectId`
- `canvasIndex`
- `viewport`
- `editorScreenshot`
- `previewScreenshot`
- `measuredRects`
- `qaResult`

학생 답이 아니라 AI가 만든 결과물의 화면 품질만 기록한다.

### Module Seams

1. `planner`
   - 교사 요청을 `ActivitySetSpec` 추천으로 바꾼다.
2. `templates`
   - 문제 은행에서 세트를 만들고 각 문제를 `CanvasActivitySpec`으로 분리한다.
3. `compiler`
   - 캔버스 하나를 MathCanvas 네이티브 payload 하나로 바꾼다.
4. `validator`
   - 세트 전체 수학 검증과 캔버스별 한 문제·레이아웃·객체 계약을 검사한다.
5. `batch authoring service`
   - 교사 승인 하나를 받아 여러 payload를 순서대로 새 프로젝트로 만들고 부분 실패를 복구한다.
6. `managed browser`
   - 로그인·계약·POST·편집 탭 열기만 담당한다.
7. `render QA`
   - 실제 편집·미리보기 화면의 rect, 글자 크기, 겹침, 드래그 가능 여부를 확인한다.

각 경계는 버전이 있는 schema와 hash로 연결한다. 화면 QA가 교육 추천이나 생성 payload를 임의로 고치지 않는다.

### Real vs Stub

#### core — 실제로 완성

- 분수 비교 한 템플릿
- 한 문제 한 캔버스
- 세트 단위 승인·생성·부분 실패 복구
- 실제 MathCanvas 생성·편집·미리보기
- 실제 드래그·문장 입력
- 1280×720·1024×768 렌더 QA
- 교사용 정답지
- macOS 현재 개발 환경

#### scaffold — 경계만 보존

- 추가 학년·성취기준 템플릿
- 학생 링크 자동 발행
- 학생 응답 수집·분석
- Windows 서명 설치 패키지와 자동 업데이트
- 원격 운영 대시보드

### Extension Points

- 새 활동 템플릿은 `ActivitySetSpec → CanvasActivitySpec[]` 계약에 연결한다.
- 막대 외 원·수직선 표현은 `visualModel` 전략으로 추가한다.
- 나중의 학생 배포는 `CreationBatch` 결과를 받는 별도 publisher로 연결하고 현재 authoring service를 바꾸지 않는다.

### State and Data Flow

`교사 요청 → 추천된 ActivitySetSpec → 교사 승인 setHash → CanvasActivitySpec[] → 개별 compile/validate → CreationBatch → MathCanvas 새 프로젝트 N개 → editorUrl[] → RenderEvidence → 사용자 최종 확인`

- `ActivitySetSpec`이 문제와 순서의 단일 기준이다.
- `CreationBatch`가 외부 쓰기의 단일 기준이다.
- MathCanvas `projectId`는 생성 성공 뒤에만 저장한다.
- 학생 응답과 개인정보는 이 흐름에 들어오지 않는다.

## Implementation Plan

### Phase 0. 실제 MathCanvas 능력 확인 — core

1. 교사가 전용 Chrome에 로그인하고 `내 캔버스`까지 들어간다.
2. 교사 승인 뒤 QA용 새 캔버스 1개만 만든다.
3. 실제 편집·학생 미리보기에서 다음을 확인한다.
   - 분수 객체의 이동과 크기·회전·분할 손잡이
   - 텍스트 입력 객체의 학생 편집 가능 여부
   - 필요한 도구만 보이게 할 수 있는지
   - 실제 스냅·놓기 기능 존재 여부
   - 실제 canvas viewBox와 viewport 축소 방식
4. 확인한 필드를 fixture와 계약 문서에 고정한다.
5. 핵심인 `이동 가능 + 변형 불가` 또는 `문장 입력 가능`을 만들 수 없으면 구현을 억지로 진행하지 않고 계획을 다시 연다.

### Phase 1. 세트·배치 계약 만들기 — core

1. `ActivitySetSpec`, `CanvasActivitySpec`, `CreationBatch`, `RenderEvidence` schema를 추가한다.
2. 기존 한 payload 승인 구조를 세트 전체 승인과 캔버스별 hash 구조로 바꾼다.
3. 부분 성공·재시작·재호출에서 중복 POST하지 않는 상태 전이를 구현한다.
4. 기존 한 캔버스 프로젝트 데이터는 읽을 수 있게 마이그레이션 경계를 둔다.

### Phase 2. 한 문제 캔버스 재설계 — core

1. 기준 Stage를 16:10으로 바꾼다.
2. 상단에 현재 분수 식과 `1/N` 순서만 둔다.
3. 가운데에 같은 폭의 두 비교 줄과 보이는 빨간 `출발선`을 둔다.
4. 식과 띠를 주황·하늘색으로 연결한다.
5. 아래에 `<`, `>` 기호 놓기와 비교 이유 문장 입력 영역을 둔다.
6. 장식·긴 설명·다른 문제·정답·통분식은 학생 화면에서 제거한다.
7. 통분식과 설명은 교사용 정답지에만 둔다.

### Phase 3. 컴파일러·validator 보완 — core

1. 한 `CanvasActivitySpec`이 한 payload만 만들게 한다.
2. 실제 MathCanvas 계약으로 이동 가능하지만 변형 불가인 분수 띠를 만든다.
3. 실제 행동이 없는 `accepts`는 payload 기능 계약에서 제거한다.
4. 문장 입력 영역을 실제 편집 가능한 객체로 만든다.
5. 한 문제 초과, 18px 미만 예상 글자, 44px 미만 조작 대상, 캔버스 이탈과 겹침을 생성 전에 차단한다.
6. 프로젝트 제목을 `{세트명} · {학년} · {난이도} · {index}/{count} [AI-{marker}]`로 고정한다.

### Phase 4. 대화·생성 흐름 보완 — core

1. 추천 응답에 세트의 문제 수·난이도·조작 방식·전체 정답지를 보여 준다.
2. 교사 승인 한 번으로 `CreationBatch`를 만든다.
3. 캔버스를 순서대로 생성하고 각 결과를 즉시 저장한다.
4. 일부 실패 시 성공 항목을 보존하고 실패 항목만 재개한다.
5. 완료되면 첫 편집 화면을 열고 전체 `editorUrl[]`을 반환한다.

### Phase 5. 자동·실화면 QA — core

1. 문제 은행 전체의 수학·난이도·중복 검사를 실행한다.
2. 2·4·6문제 세트의 compile·batch·재시작·부분 실패 테스트를 추가한다.
3. 현재 작업본과 깨끗한 clone에서 typecheck·74개 기존 테스트·새 테스트·build를 실행한다.
4. 실제 MathCanvas에 대표 보통 4문제 세트를 생성한다.
5. 네 캔버스 모두에서 편집 화면과 학생 미리보기를 캡처한다.
6. 1280×720과 1024×768에서 글자·대상 크기·겹침·잘림을 실제 rect로 잰다.
7. 각 캔버스에서 띠 두 개와 기호를 옮기고 문장 입력이 가능한지 확인한다.
8. 기존 프로젝트가 바뀌지 않았고 중복 프로젝트가 생기지 않았는지 확인한다.

### Phase 6. 사용자 결과 확인 — core

1. 실제 생성된 네 편집 URL과 미리보기 캡처를 사용자에게 보여 준다.
2. 수학·화면·조작·안전 체크리스트 결과를 함께 제시한다.
3. 비교 서비스와 같은 기준으로 활동지 결과물을 다시 점검하되 점수보다 P0 체크리스트 통과 여부를 우선한다.
4. 사용자가 결과물을 직접 보고 승인하거나 보완을 지시한다.
5. 승인 뒤에만 다교사 설치·배포 계획을 시작한다.

## Risk Ledger

| 위험 | 잘못되면 생기는 일 | 계획 대응 |
|---|---|---|
| 문제 전환 없음 | 한 캔버스에 다시 몰아넣어 가독성 실패 | 문제별 새 캔버스와 세트·배치 모델 |
| 여러 POST 중 부분 실패 | 일부만 생기거나 재시도 때 중복 생성 | item별 hash·상태·projectId를 저장하고 빠진 항목만 재개 |
| 분수 객체 변형 손잡이 | 학생이 길이를 바꿔 수학 과제가 무너짐 | Phase 0 계약 게이트, 해결 불가 시 구현 중단 |
| 문장 입력 불가 | 선택한 활동 구조를 충족하지 못함 | Phase 0 계약 게이트, 임의의 말하기 활동으로 바꾸지 않음 |
| `accepts`가 의미 모델에만 존재 | 없는 스냅·판정 기능을 지원하는 것처럼 보임 | 실제 행동이 아니면 계약·문서에서 제거 |
| 캔버스 수 증가 | 교사 목록이 복잡해짐 | 공통 세트명, `index/count`, URL 순서 반환 |
| 내부 API 변경 | 전 교사 생성 중단 | fail-closed 계약 검사와 버전 fixture 유지 |
| QA가 학생 채점으로 확대 | 제품 범위와 사용자의 성공 기준이 흐려짐 | 생성물 렌더·조작만 QA하고 학생 답은 다루지 않음 |

## Risk Review Change Log

- 한 캔버스 4문제 → 한 요청에서 한 문제짜리 새 캔버스 N개 생성 (addresses: 문제 전환 부재·가독성)
- 자기 확인·교사 확인 설계 → 학생 채점과 확인 흐름을 범위에서 제거 (addresses: 목표 불일치)
- 교사·학생 인원 파일럿 → 실제 생성물 체크리스트와 사용자 승인 (addresses: 성공 기준 불일치)
- `accepts`를 드롭 기능처럼 취급 → 실제 MathCanvas 행동일 때만 기능 계약에 포함 (addresses: 통합 불확실성)
- 학생 반응 검수 → AI가 만든 렌더·드래그·입력 결과만 QA (addresses: 책임 범위 혼동)

## Implementation Prompt

### Role and Objective

당신은 기존 `mathcanvas-ai-authoring` 저장소를 보완하는 시니어 TypeScript·교육용 인터랙션 엔지니어다. Chrome 확장 프로그램과 Computer Use 없이 Codex·Claude Code 대화로 MathCanvas 새 캔버스를 만드는 현재 MCP 구조를 유지하면서, 분모가 다른 분수 비교 활동을 실제 교사가 결과물로 판단할 수 있는 품질로 다시 만든다.

핵심 목표는 `한 요청 → 교사 승인 → 한 문제짜리 새 캔버스 N개 → 첫 편집 화면과 전체 URL 반환`이다. 학생 답을 채점하거나 교사 운영을 자동화하지 않는다. AI가 만든 캔버스 자체의 수학·화면·드래그·입력 품질만 끝까지 검증한다.

### Background

현재 구현은 코드 검사와 74개 테스트를 통과하지만, 대표 4문제를 `2400×2900` 한 캔버스에 넣어 1024px 화면에서 놓기 칸 라벨이 약 10px로 줄어든다. `ActivitySpec.accepts`는 실제 MathCanvas 드롭 행동으로 컴파일되지 않으며, 인증된 계정에서 실제 POST·렌더·드래그·입력을 아직 확인하지 않았다.

MathCanvas는 한 프로젝트 안의 문제 전환을 지원하지 않는다. 따라서 여러 문제를 한 캔버스에 다시 몰아넣지 말고 문제별 새 캔버스 세트로 만든다.

### Target Users

- 제작·운영: 일반 초등 교사
- 학습자: 초등 5학년 권장, 학생 1명당 데스크톱 기기 1대
- AI 환경: Codex 또는 Claude Code
- 지원 운영체제: macOS와 Windows 데스크톱

### Platform and Environment

- TypeScript·pnpm 모노레포
- stdio MCP 서버
- Playwright 기반 별도 영구 Chrome 프로필
- MathCanvas origin: `https://mathcanvas.vivasam.com`
- 현재 내부 API와 네이티브 객체 계약은 공식 SDK가 아니므로 fail-closed를 유지한다.
- Chrome 확장 프로그램, loopback 서버, 연결 코드, Computer Use를 추가하지 않는다.

### Scope for This Version

- `[6수01-07]` 분모가 다른 분수의 크기 비교 템플릿 하나
- 2~6문제 세트
- 쉬움·보통·어려움
- 한 문제 한 캔버스
- 분수 띠 두 개, 공통 출발선, `<`·`>` 기호, 비교 이유 문장 입력 영역
- 세트 단위 추천·승인·생성·부분 실패 복구
- 실제 MathCanvas 편집·학생 미리보기 QA
- 첫 편집 화면 열기와 전체 편집 URL 반환

### Non-Goals

- 학생 정답·오답 판정과 자동 피드백
- 자기 확인·교사 확인 운영 흐름
- 학생 응답 수집·분석·대시보드
- 학생 링크 자동 발행
- 기존 프로젝트 수정·삭제
- 로그인 자동 처리
- 검증되지 않은 수학 주제 자유 생성
- 이번 단계의 공개 배포와 자동 업데이트

### Core User Flow

1. 교사가 전용 Chrome을 열어 MathCanvas에 로그인한다.
2. 교사가 대화로 학년·문제 수·난이도를 요청한다.
3. AI가 교육과정, 조작 방식, 문제 수와 교사용 정답지를 추천한다.
4. 교사가 추천안을 승인한다.
5. 서버가 `ActivitySetSpec`과 `setHash`를 고정한다.
6. 문제별 `CanvasActivitySpec`과 payload를 만들고 각각 검증한다.
7. `CreationBatch`가 새 프로젝트를 순서대로 생성한다.
8. 일부 실패하면 성공한 projectId를 보존하고 빠진 항목만 재개한다.
9. 완료되면 첫 편집 탭을 열고 모든 편집 URL과 `1/N~N/N` 순서를 반환한다.
10. 실제 미리보기에서 생성물 렌더·드래그·입력을 QA하고 증거를 남긴다.

### Ordered Functional Requirements

#### R1. 실제 계약 탐색 — core

- 교사 승인 뒤 QA용 새 캔버스 1개로 분수 객체와 입력 객체의 실제 행동을 확인한다.
- 이동 가능하면서 크기·회전·분할을 막을 수 있는지 확인한다.
- 학생 미리보기에서 텍스트 입력이 가능한지 확인한다.
- 스냅이 실제로 있는지 확인하고 없으면 기능 계약과 문서에서 제거한다.
- 핵심 동작이 불가능하면 임의의 우회 구현을 하지 말고 작업을 중단해 계획을 다시 연다.

#### R2. 세트 데이터 계약 — core

- `ActivitySetSpec`, `CanvasActivitySpec`, `CompiledCanvasProject`, `CreationBatch`, `RenderEvidence` schema를 구현한다.
- 한 `CanvasActivitySpec`에는 문제가 정확히 하나만 들어가야 한다.
- 세트와 캔버스별 hash를 분리한다.
- schema·상태 파일에 버전을 둔다.

#### R3. 한 문제 캔버스 템플릿 — core

- 기준 Stage는 16:10이다.
- 현재 분수 식과 `index/count`를 상단에 둔다.
- 가운데에 같은 폭의 두 비교 줄과 보이는 빨간 `출발선`을 둔다.
- 첫째 분수 식·띠는 주황, 둘째 분수 식·띠는 하늘색으로 연결한다.
- `<`, `>` 기호 놓기와 비교 이유 문장 입력 영역을 둔다.
- 학생 화면에는 정답·통분식·긴 설명·다른 문제를 넣지 않는다.
- 교사용 정답지에는 비교식·통분식·띠 설명을 유지한다.

#### R4. MathCanvas 컴파일 — core

- 한 캔버스 사양을 한 payload로 컴파일한다.
- 실제로 지원되는 이동·입력 행동만 payload에 넣는다.
- 실제 동작이 없는 `accepts`를 스냅·판정 기능처럼 남기지 않는다.
- 프로젝트 제목은 공통 세트명과 `index/count`를 포함한다.
- 각 payload hash가 승인된 세트와 문제에서 결정적으로 재현되어야 한다.

#### R5. 생성 전 validator — core

- 한 캔버스 한 문제를 강제한다.
- 교육과정 코드·학년군·이분모·비동치·정답·난이도 차이를 검사한다.
- 같은 전체 폭과 출발선 좌표를 검사한다.
- 캔버스 이탈, 고정 객체 겹침, 학생 글자·조작 대상 최소 크기를 검사한다.
- 네이티브 객체 계약과 민감 정보 부재를 검사한다.

#### R6. 세트 승인과 배치 생성 — core

- 교사 승인 한 번이 세트 전체에만 적용되게 한다.
- 각 item의 queued·creating·succeeded·failed·expired 상태를 저장한다.
- item별 payloadHash, projectId, editorUrl을 원자적으로 기록한다.
- 재시작·재호출·부분 실패에서 중복 POST하지 않는다.
- 이미 성공한 item은 다시 만들지 않는다.

#### R7. MCP 응답과 편집 화면 — core

- 추천 응답에 세트 요약과 전체 교사용 정답지를 보여 준다.
- 생성 완료 뒤 첫 편집 화면을 활성화한다.
- 전체 캔버스의 번호·문제·projectId·editorUrl을 순서대로 반환한다.
- 실패 시 성공 항목과 실패 항목을 분리해 민감 정보 없이 안내한다.

#### R8. 실제 화면 QA — core

- 실제 MathCanvas 편집 화면과 학생 미리보기를 각각 캡처한다.
- 1280×720과 1024×768에서 DOM·SVG·Canvas rect와 실제 글자 크기를 잰다.
- 잘림·겹침 0개, 학생 글자 18px 이상, 조작 대상 44×44px 이상을 강제한다.
- 각 캔버스에서 분수 띠 두 개와 기호를 실제로 옮기고 문장 입력을 확인한다.
- 학생 답의 내용이나 정오를 검사하지 않는다.

#### R9. 자동 회귀 QA — core

- 기존 74개 테스트를 보존한다.
- 2·4·6문제 세트와 세 난이도의 수학·컴파일 테스트를 추가한다.
- 배치 부분 실패·재시작·동일 승인 재호출·상태 파일 손상 테스트를 추가한다.
- 현재 작업본과 깨끗한 GitHub clone에서 typecheck·test·build를 통과한다.
- production dependency audit를 통과한다.

#### R10. 사용자 결과 제출 — core

- 대표 보통 4문제 세트를 실제 MathCanvas에 생성한다.
- 네 편집 URL과 편집·미리보기 캡처를 사용자에게 보여 준다.
- 수학·화면·조작·안전 체크리스트를 함께 제출한다.
- 점수보다 P0 체크리스트 전부 통과 여부를 우선한다.
- 사용자가 실제 생성물을 보고 최종 성공 여부를 판단한다.

### Content and Data Requirements

- 공식 교육부 `[6수01-07]` 문구를 기준으로 한다.
- `DECK6/korean-elementary-learning-map`은 현재 고정 커밋만 보조 자료로 사용한다.
- 문제 은행의 모든 쌍은 분모가 다르고 크기가 같지 않아야 한다.
- 쉬움 28~56%, 보통 15~27%, 어려움 8~14.5%의 시각 차이 범위를 유지한다.
- 같은 seed·템플릿 버전·요청은 동일한 문제와 hash를 만들어야 한다.
- 학생 개인정보와 학생 입력값을 MCP 상태 파일에 저장하지 않는다.

### Visual and UX Direction

- 선택 스타일: `집중형 비교 실험실`
- 한 화면 한 문제, 낮은 정보 밀도, 분수 띠 중심
- 주황·하늘색 분수, 빨간 출발선, 흰색·옅은 하늘색 배경
- 식과 띠의 색을 직접 일치시킨다.
- 큰 분수, 한 줄 지시, 큰 드래그 영역을 사용한다.
- 장식, 긴 설명, 여러 문제, 작은 라벨, 불필요한 도구 막대를 피한다.

### Architectural Backbone

- 단일 기준: `ActivitySetSpec`
- 문제별 출력: `CanvasActivitySpec[]`
- 외부 쓰기 기준: `CreationBatch`
- 실제 결과 증거: `RenderEvidence`
- 모듈 경계: planner → templates → compiler → validator → batch service → managed browser → render QA
- 확장 경계: 새 템플릿, 새 시각 모델, 향후 publisher

### Depth Tags

- `core`: 실제 계약 탐색, 세트 모델, 한 문제 템플릿, compiler, validator, batch 생성, MCP 응답, 실제 화면 QA, 자동 테스트
- `scaffold`: 추가 템플릿, 학생 배포, 응답 분석, Windows 서명 패키지, 원격 운영 기능

### Privacy and Safety Constraints

- MathCanvas token과 cookie를 페이지 컨텍스트 밖으로 반환하지 않는다.
- Authorization 헤더와 비밀번호를 로그·응답·상태 파일에 저장하지 않는다.
- 외부 쓰기는 교사 승인 뒤 새 프로젝트 POST만 허용한다.
- 기존 프로젝트 수정·삭제 endpoint와 일반 브라우저 제어를 추가하지 않는다.
- 계약 불일치, validator 오류, hash 불일치에서는 생성하지 않는다.

### Definition of Done per Core Unit

- 세트 모델: 2·4·6문제에서 문제 순서와 hash가 결정적이며 한 캔버스 한 문제가 schema로 강제된다.
- 템플릿: 실제 1024×768에서도 모든 학생 요소가 읽히고 겹치지 않는다.
- 컴파일러: 실제 MathCanvas에서 의도한 객체가 렌더되고 허용한 것만 움직인다.
- validator: 고의로 만든 수학·배치·계약 오류를 각각 실패시킨다.
- 배치 생성: 중간 실패와 프로세스 재시작 뒤 중복 없이 완료한다.
- 실제 화면 QA: 편집·미리보기에서 렌더·드래그·입력을 모두 증명한다.
- 안전: 기존 프로젝트 변경과 민감 정보 노출이 0건이다.

### QA Path

1. 정적 content audit
2. typecheck·unit·integration·build
3. clean clone 재현
4. 공개 계약 검사
5. 인증된 QA용 새 캔버스 1개 계약 탐색
6. 대표 4문제 세트 실제 생성
7. 편집·미리보기 두 viewport 캡처와 rect 검사
8. 실제 드래그·입력
9. 기존 프로젝트 불변·중복 부재 확인
10. 사용자에게 실제 결과 제출

### Open Questions and Assumptions

- blocking open question은 현재 없다.
- 실제 계약 탐색에서 이동 전용 분수 객체나 편집 가능한 문장 입력을 만들 수 없으면 작업을 멈추고 계획을 다시 연다.
- 스냅은 확인된 경우에만 구현한다.
- 학생 답 판정과 학생 배포는 이번 버전의 성공 기준이 아니다.

## Audit Baseline

- 기술 구현: 76/100
- 활동지 결과물: 56/100
- 여러 교사 배포 준비도: 31/100
- 종합 출시 준비도: 59/100
- 현재 대표 payload: `2400×2900`, 4문제, 네이티브 객체 59개, 최소 12회 드래그
- 현재 1024px 폭 환산: 큰 지시문 약 20.5px, 놓기 칸 라벨 약 10.2px
- 현재 실서비스 스모크: 관리형 Chrome 실행 성공, 인증 상태는 `login-required`

---

## 기존 구현 기준

## 확정 범위

- 교사는 Codex 또는 Claude Code와 대화한다.
- Chrome 확장 프로그램과 Computer Use는 사용하지 않는다.
- 로컬 stdio MCP 서버가 설치된 Google Chrome을 별도 영구 프로필로 실행한다.
- 교사는 해당 전용 창에서 MathCanvas에 직접 로그인하고 `내 캔버스`까지 이동한다.
- AI는 검증된 새 프로젝트만 생성하며 기존 프로젝트는 수정·삭제하지 않는다.
- 성공하면 같은 전용 Chrome에 MathCanvas 편집 화면을 연다.
- macOS와 Windows를 지원한다.
- 첫 검증 활동은 초등 5학년 `분모가 다른 분수의 크기 비교`다.

## 아키텍처 백본

`교사 대화 → Codex/Claude Code → stdio MCP → 추천·컴파일·검증 → 관리형 Chrome 런타임 → MathCanvas 페이지 컨텍스트 → 새 프로젝트 → 편집 탭`

MathCanvas 토큰은 페이지 컨텍스트의 지역 변수로만 읽고 사용한다. MCP 응답, 로컬 작업 파일, 로그에는 토큰과 Authorization 헤더를 남기지 않는다.

## 구현 단위와 완료 기준

### U1. 관리형 Chrome 런타임 — core

- 전용 user-data-dir로 Google Chrome을 실행한다.
- MathCanvas 홈을 열고 로그인 상태를 진단한다.
- 생성 직전 공개 객체 계약과 인증 계약을 다시 검사한다.
- 승인 payload 해시를 Node와 페이지에서 각각 검사한다.
- 성공 시 `/ko/view/{projectId}`를 새 활성 탭으로 연다.
- 브라우저 실행 실패·로그인 필요·계약 불일치를 민감 정보 없이 구분한다.

### U2. MCP 승인·생성 흐름 — core

- 전용 창 열기, 연결 확인, 추천, 생성, 상태 확인 도구를 제공한다.
- 추천과 생성 사이에 교사 명시 승인과 동일 해시를 요구한다.
- 작업을 외부 쓰기 전에 원자적으로 저장한다.
- 같은 승인은 재시작·재호출 뒤 중복 POST하지 않는다.
- 새 추천을 새로 승인하면 새 캔버스를 만든다.

### U3. 설치·온보딩 — core

- macOS와 Windows 설치 스크립트가 Codex와 Claude Code MCP 등록을 지원한다.
- 확장 프로그램, 개발자 모드, 연결 코드, 로컬 포트를 요구하지 않는다.
- 첫 대화부터 로그인 완료 위치까지 교사용 한국어 안내를 제공한다.
- Chrome과 빌드 상태를 진단하는 doctor를 제공한다.

### U4. 교육 콘텐츠 파이프라인 — core/보존

- 기존 교육과정 resolver, 템플릿, 컴파일러, validator를 유지한다.
- 같은 전체·같은 출발선·최소 8% 시각 차이와 교사용 정답지를 유지한다.

### U5. 향후 확장 경계 — scaffold

- 추가 활동 템플릿, 학생 배포, 원격 추천은 인터페이스만 유지한다.
- 가짜 네트워크 구현이나 배포 UI는 추가하지 않는다.

## 완료 체크리스트

- [x] 확장 프로그램 소스와 빌드 설정 0개
- [x] 연결 코드·loopback HTTP 브리지·브리지 포트 0개
- [x] 관리형 Chrome 단위 테스트
- [x] 승인부터 편집 URL까지 통합 테스트
- [x] 실제 설치된 Chrome으로 전용 프로필 실행 스모크
- [x] 타입 검사·전체 테스트·production build·의존성 감사
- [x] macOS 설치 스크립트 구문 검사
- [ ] Windows 스크립트 정적 검사
- [x] 문서와 코드의 구조 일치
- [x] Claude Opus 5 독립 검토와 보수적 점수
- [x] 비공개 GitHub 저장소 `main` 갱신

## 실서비스 경계

로그인, CAPTCHA, 2단계 인증은 교사가 전용 Chrome에서 직접 처리한다. 사용자 계정에 실제 새 프로젝트를 쓰는 검증은 전용 프로필 로그인이 완료된 뒤에만 수행한다. 로그인되지 않은 상태를 실서비스 생성 성공으로 보고하지 않는다.
