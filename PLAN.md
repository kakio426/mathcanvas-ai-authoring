# MathCanvas AI 활동지 보완 계획

계획 상태: **v2.4.1 구현·실화면 QA 완료, 사용자 확인 대기**

## 2026-07-29 재개 사유

사용자 실화면 검토에서 다음 결함이 확인됐다.

- 학생이 어디를 눌러 글을 써야 하는지 알 수 없음
- 드래그 띠에는 네이티브 분수 모형을 썼지만, 분수 카드에는 수식 입력기의
  분수 템플릿을 쓰지 않음
- `1710×895` 화면의 기본 60% 보기에서 활동판 폭이 약 669px인데도
  실제 사용 면적을 검증하지 않음

상세 메뉴 감사와 새 제작 규칙은 `MATHCANVAS_MENU_AUDIT.md`를 따른다.
v2.3.0 네 캔버스는 실패 재현본으로 보존한다. v2.4.1은 별도 새
캔버스로 만들며 기존 캔버스를 수정하거나 삭제하지 않는다.

## 목표와 현재 범위

- 일반 초등 교사가 Codex 또는 Claude Code와 대화만으로 활동지를 추천받고
  새 MathCanvas 캔버스를 생성한다.
- Chrome 확장 프로그램과 Computer Use는 쓰지 않는다.
- 교사는 전용 Chrome에서 직접 로그인한다.
- AI는 새 캔버스만 만들고 기존 캔버스는 수정·삭제하지 않는다.
- 생성 완료 지점은 MathCanvas 편집 화면이다. 학생 미리보기·학생 게시·
  자동 채점·오답 피드백·응답 수집은 이번 범위가 아니다.
- 첫 검증 활동은 5학년 `[6수01-07]` 분모가 다른 분수의 크기 비교다.

## 학습 활동 계약

### 학습 목표

같은 전체로 만든 두 분수 띠를 같은 출발선에 놓아 길이를 비교하고,
분모가 다른 분수의 크기 관계를 기호와 문장으로 나타낸다.

### 학생 행동

1. 시작점이 다른 두 띠를 같은 출발선에 옮긴다.
2. 길이를 보고 `<` 또는 `>`를 관계 칸에 옮긴다.
3. 더 긴 띠와 그 까닭을 한 줄로 쓴다.

수학적 판단은 띠의 공통 시작점, 길이 관계, 관계 기호의 세 가지다.
띠의 준비 위치만 보아 정답을 고를 수 없도록, 준비 상태의 오른쪽 끝은
실제 크기 관계와 반대 인상을 주는 `productive conflict`로 배치한다.

### 화면 계약

- `1280×800`, `16:10`, 한 캔버스 한 문제
- 1순위: 띠 준비 상자와 공통 출발선 비교 자리
- 2순위: 분수 관계식과 기호
- 3순위: 까닭 입력
- 읽는 분수는 수식 메뉴 계약의 `math-latex` 분수식 한 객체로 만든다.
- 분수·숫자·기호의 실제 `math-field` 중심과 카드 중심의 가로·세로
  오차는 각각 1px 이하로 제한한다.
- 분자·가로선·분모를 서로 다른 객체로 조립하는 방식은 차단한다.
- 출발선 안내와 출발선 이름은 서로 겹치지 않는다.
- 글쓰기 안내와 노란 빈 입력칸 사이에는 최소 16px 간격을 둔다.
- 데스크톱 첫 화면은 `canvasOption.scale=2`, 즉 120%로 연다.
- 하단 도구막대와 응답 패널은 실제 화면에서 겹치지 않는다.
- 본문과 조작 대상은 실제 렌더에서 읽고 누를 수 있는 크기를 유지한다.

### 제품 제약

MathCanvas 한 캔버스에서 문제 전환을 지원하지 않으므로, 4문제 요청은
한 문제짜리 새 캔버스 4개로 만든다. 스냅·정답 판정·오답 피드백은
지원되는 것처럼 표시하지 않는다.

## 기술 구조

`교사 요청 → 추천 ActivitySetSpec → 교사 승인 setHash →
CanvasActivitySpec[] → compile/validate → CreationBatch →
MathCanvas 신규 프로젝트 N개 → editorUrl[] → 실화면 QA`

- `ActivitySetSpec`: 문제·난이도·순서의 단일 기준
- `CanvasActivitySpec`: 한 캔버스 한 문제
- `CompiledCanvasProject`: 검증된 payload와 해시
- `CreationBatch`: 부분 실패 재개와 중복 생성 방지
- `RenderEvidence`: 편집 화면의 rect·캡처·조작 결과

## 검증 게이트

1. 공식 교육과정과 Middle of Math 기준으로 문제·정답·설명을 검사한다.
2. 컴파일 전 validator가 수학 오류, 캔버스 이탈, 겹침, 분수 중심축,
   준비 위치의 정답 노출을 차단한다.
3. typecheck·105개 테스트·전체 build를 통과한다.
4. 최종 캔버스 4개를 실제 교사 Chrome `1710×895`, DPR 2에서
   새로 불러와 초기 상태를 검사한다.
5. 별도 QA 캔버스에서 띠 2개·기호를 실제로 옮기고 까닭을 입력한다.
6. 화면 크기 변경 뒤에는 페이지를 다시 불러와 해당 viewport 레이아웃인지
   확인한다.
7. Humanizer로 학생 문구를 초등학생이 바로 이해하는 말로 검사한다.
8. Codex 단독으로 현재 코드와 최신 실화면 증거를 다시 검토한다.
9. 최종 기본 120%에서 학생 입력 위치의 첫 클릭 전 상태를 검사한다.
10. 네이티브 분수 모형·수식 입력기 사용 여부를 검사한다.
11. 작성 좌표나 SVG 그룹이 아니라 실제 `math-field`와 카드의 중심 오차를
    재고 1px을 넘으면 생성·실화면 QA를 실패시킨다.

## 성공 기준

- 최종 4개 `1710×895`, DPR 2 초기 화면 PASS
- 네 캔버스 모두 수식 분수 4개, 조립 분수 조각 0개
- 네 캔버스의 분수·기호 실제 중심 오차 가로·세로 각각 1px 이하
- 별도 QA 캔버스의 띠 2개·기호 이동과 글쓰기 PASS
- 도구막대·학습 행·응답 패널 겹침 0건
- 준비 상태의 띠 끝점이 정답을 미리 알려 주지 않음
- 기존 캔버스 수정·삭제 0건, 학생 게시 0건
- 자동 검사와 독립 심사에 P0·P1 미해결 항목 0건
- 3초 보기에서 학생이 글쓰기 위치를 바로 가리킴
- 분수 표현과 조작이 MathCanvas 네이티브 기능을 우선 사용함
- 기본 120%에서 비교판 실제 폭 1180px
- 사용자가 실제 편집 화면을 보고 최종 승인

## 배포 전 별도 게이트

- Windows 실기기 Codex·Claude Code E2E
- macOS 깨끗한 사용자 계정 설치
- 서명 패키지·체크섬·제거·롤백
- MathCanvas 계약 변경 감시
- 사용자 승인 뒤 3~5명 교사 비공개 파일럿

이 배포 게이트는 현재 활동지 제작 결과의 결함과 구분해 기록한다.
