# 확장 프로그램 없는 MathCanvas AI 제작 도구 전환 계획

## 확정 범위

- 교사는 Codex 또는 Claude Code와 대화한다.
- Chrome 확장 프로그램과 Computer Use는 사용하지 않는다.
- 로컬 stdio MCP 서버가 설치된 Google Chrome을 별도 영구 프로필로 실행한다.
- 교사는 해당 전용 창에서 MathCanvas에 직접 로그인하고 `내 캔버스`까지 이동한다.
- AI는 검증된 새 프로젝트만 생성하며 기존 프로젝트는 수정·삭제하지 않는다.
- 성공하면 같은 전용 Chrome에 MathCanvas 편집 화면을 연다.
- macOS와 Windows를 지원한다.
- 첫 검증 활동은 초등 5학년 `분모가 다른 분수의 크기 비교`다.

## 아키텍처 백본

`교사 대화 → Codex/Claude Code → stdio MCP → 추천·컴파일·검증 → 관리형 Chrome 런타임 → MathCanvas 페이지 컨텍스트 → 새 프로젝트 → 편집 탭`

MathCanvas 토큰은 페이지 컨텍스트의 지역 변수로만 읽고 사용한다. MCP 응답, 로컬 작업 파일, 로그에는 토큰과 Authorization 헤더를 남기지 않는다.

## 구현 단위와 완료 기준

### U1. 관리형 Chrome 런타임 — core

- 전용 user-data-dir로 Google Chrome을 실행한다.
- MathCanvas 홈을 열고 로그인 상태를 진단한다.
- 생성 직전 공개 객체 계약과 인증 계약을 다시 검사한다.
- 승인 payload 해시를 Node와 페이지에서 각각 검사한다.
- 성공 시 `/ko/view/{projectId}`를 새 활성 탭으로 연다.
- 브라우저 실행 실패·로그인 필요·계약 불일치를 민감 정보 없이 구분한다.

### U2. MCP 승인·생성 흐름 — core

- 전용 창 열기, 연결 확인, 추천, 생성, 상태 확인 도구를 제공한다.
- 추천과 생성 사이에 교사 명시 승인과 동일 해시를 요구한다.
- 작업을 외부 쓰기 전에 원자적으로 저장한다.
- 같은 승인은 재시작·재호출 뒤 중복 POST하지 않는다.
- 새 추천을 새로 승인하면 새 캔버스를 만든다.

### U3. 설치·온보딩 — core

- macOS와 Windows 설치 스크립트가 Codex와 Claude Code MCP 등록을 지원한다.
- 확장 프로그램, 개발자 모드, 연결 코드, 로컬 포트를 요구하지 않는다.
- 첫 대화부터 로그인 완료 위치까지 교사용 한국어 안내를 제공한다.
- Chrome과 빌드 상태를 진단하는 doctor를 제공한다.

### U4. 교육 콘텐츠 파이프라인 — core/보존

- 기존 교육과정 resolver, 템플릿, 컴파일러, validator를 유지한다.
- 같은 전체·같은 출발선·최소 8% 시각 차이와 교사용 정답지를 유지한다.

### U5. 향후 확장 경계 — scaffold

- 추가 활동 템플릿, 학생 배포, 원격 추천은 인터페이스만 유지한다.
- 가짜 네트워크 구현이나 배포 UI는 추가하지 않는다.

## 완료 체크리스트

- [x] 확장 프로그램 소스와 빌드 설정 0개
- [x] 연결 코드·loopback HTTP 브리지·브리지 포트 0개
- [x] 관리형 Chrome 단위 테스트
- [x] 승인부터 편집 URL까지 통합 테스트
- [x] 실제 설치된 Chrome으로 전용 프로필 실행 스모크
- [x] 타입 검사·전체 테스트·production build·의존성 감사
- [x] macOS 설치 스크립트 구문 검사
- [ ] Windows 스크립트 정적 검사
- [x] 문서와 코드의 구조 일치
- [ ] Claude Opus 5 독립 검토와 보수적 점수
- [ ] 비공개 GitHub 저장소 `main` 갱신

## 실서비스 경계

로그인, CAPTCHA, 2단계 인증은 교사가 전용 Chrome에서 직접 처리한다. 사용자 계정에 실제 새 프로젝트를 쓰는 검증은 전용 프로필 로그인이 완료된 뒤에만 수행한다. 로그인되지 않은 상태를 실서비스 생성 성공으로 보고하지 않는다.
