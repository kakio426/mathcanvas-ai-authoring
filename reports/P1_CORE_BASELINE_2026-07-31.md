# P1 core baseline refresh — 2026-07-31

The baseline is refreshed after the previously validated balance-scale compiler support began linking generated plate members to their item scale in `compile-resolved.ts`.

- The Wave 9 recommendation fix does not change a frozen core file; `resolve-activity.ts` is restored to its prior hash.
- The retained core difference is the generic balance-scale cardinality and `plate` linkage required by the released Wave 5 balance-scale activity.
- The change contains no activity ID, curriculum code, Korean domain literal, or activity-specific role.
- The balance-scale release canary remains the evidence for the retained compiler behavior.
