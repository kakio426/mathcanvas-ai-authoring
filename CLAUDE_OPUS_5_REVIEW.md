# Claude Opus 5 독립 심사

심사일: 2026-07-29

## 최초 심사 — ITERATE

로컬 Claude CLI에서 `--model claude-opus-5`를 명시해 읽기 전용으로
실행했습니다. 반환 메타데이터의 canonical model은 `claude-opus-5`였고,
기술 **74/100**, 활동지 **66/100**, 판정 **ITERATE**였습니다.

### 최초 심사 핵심 지적

- P0 1건: 띠 준비 위치가 일부 문제에서 정답을 시각적으로 암시하고,
  정렬하지 않고도 답할 수 있음
- P1 6건: 분수 중심 정렬, stale viewport 증거, 최종본 조작 증거 과장,
  범위와 맞지 않는 학생 미리보기 요구, 고정 라벨 겹침, 입력 DOM 잡음
- P2: 띠 폭 반올림 차이, 입력 간격, 오래된 계획·체크리스트

최초 실패 판정은 폐기하지 않고 이 문서와
`qa/_archive/2026-07-29-opus5-initial-iterate/`에 보존합니다.

## v2.3.0 보완

1. 준비 상태의 띠 끝점이 실제 관계와 반대 인상을 주도록 배치하고,
   그렇지 않으면 validator가 생성을 중단합니다.
2. 분자·가로선·분모의 중심축과 세로 간격을 고정하고 실제 glyph 중심을
   브라우저에서 측정합니다.
3. `1630×1122` 조작 전에 reload하고 정확 viewport와 새 레이아웃을
   모두 확인합니다.
4. 최종 4개는 읽기 전용 증거, 별도 v2.3.0 검증본 4개는 모두 전체
   조작 증거로 명확히 구분합니다.
5. 현재 제품 범위를 MathCanvas 편집 화면까지로 바로잡았습니다.
6. 목표 안내·출발선 이름·출발선의 rect를 분리했습니다.
7. 실제 `.text-edit`만 읽어 정확한 입력 문장만 기록합니다.
8. 띠 폭·입력 간격·문서의 오래된 수치를 바로잡았습니다.

## 재심 — PASS

- 실행 모델 지정: `--model claude-opus-5`
- 반환 canonical model: `claude-opus-5`
- 판정: **PASS**
- 기술 점수: **92/100**
- 활동지 점수: **90/100**
- 미해결 P0: **0건**
- 미해결 P1: **0건**
- 최초 A~G 지적: **7건 모두 RESOLVED**

재심은 현재 코드, 100개 테스트, `final-v230` 초기 화면 12개,
`opus-remediation-v230` 4개 전체 조작 증거와 사용자 실패 원본을
읽기 전용으로 비교했습니다.

### 지적별 재심 근거

| 지적 | 판정 | 핵심 근거 |
|---|---|---|
| A 준비 위치가 답을 암시 | RESOLVED | 반대 끝점 단서, validator, 4개 조작 metrics |
| B 분수 중심 불량 | RESOLVED | 분자·선·분모 중심축과 실제 glyph ±3px 검사 |
| C stale viewport | RESOLVED | resize 뒤 reload, exact viewport, fresh layout |
| D 최종 조작 과장 | RESOLVED | final은 `skipped:true`, 별도 4개만 전체 조작 |
| E 학생 미리보기 요구 | RESOLVED | 현재 범위를 편집 화면까지로 일관되게 정리 |
| F 고정 라벨 8px 겹침 | RESOLVED | rect 교차 0, validator와 실화면 assertion |
| G 입력 DOM 잡음 | RESOLVED | `.text-edit` 값만 읽고 정확한 네 문장 기록 |

### Opus 5가 확인한 강점

- 사용자 원본의 분수 넘침, 띠·출발선 겹침, 목적지 불명확이 모두 사라짐
- 네 조작본에서 띠 2개·정답 기호·정확한 입력·exact/fresh viewport 통과
- 준비 위치가 오개념을 드러내고, 공통 출발선 정렬로 이를 반증함
- 같은 전체·같은 출발선·난이도·정답 노출 금지를 validator가 fail-closed로 강제
- 분수 띠는 이동만 가능하고 크기·회전·분할로 답을 바꿀 수 없음
- 사용자 제보 화면 크기가 영구 회귀 viewport로 고정됨

### 남은 P2

1. Windows에서는 macOS용 `Meta+A`를 쓰는 현재 실화면 QA 스크립트를
   그대로 재현할 수 없습니다.
2. 목표 안내와 출발선 이름은 교차하지 않지만 실제 렌더의 세로 여유가
   약 3.5px로 작습니다.

Opus 5가 지적한 `pnpm audit`·Chrome doctor·smoke 재실행은 재심 직후
모두 실제로 다시 실행해 PASS로 갱신했습니다.

### 점수 근거

기술 92점은 승인·해시·정규 재컴파일, fail-closed validator,
exact/fresh viewport 실서비스 QA, 읽기 전용본과 조작본 분리,
stdio-only와 새 프로젝트 전용 안전 경계를 반영한 점수입니다.

활동지 90점은 수학적으로 정확한 문제, 같은 전체·공통 출발선,
준비 위치의 반대 단서, 기호와 설명으로 이어지는 흐름, 세 화면 크기의
겹침·넘침 0건을 반영한 점수입니다. 템플릿 범위가 한 개뿐이고 세 행동이
한 화면에 함께 보이는 점은 감점 요인입니다.
