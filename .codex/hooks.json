{
  "description": "Fail-closed guard for all MathCanvas project writes, portfolio release attestation, and lesson-bundle link promotion.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^Bash$",
        "hooks": [
          {
            "type": "command",
            "command": "node \"$(git rev-parse --show-toplevel)/scripts/hooks/mathcanvas-harness-guard.mjs\"",
            "timeout": 5,
            "statusMessage": "Checking the MathCanvas learning-design and live-write boundary"
          }
        ]
      }
    ]
  }
}
