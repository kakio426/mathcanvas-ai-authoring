# 구현·검증 보고서

검증일: 2026-07-29

## 구현 결과

- TypeScript·pnpm 모노레포
- Codex·Claude Code 공용 stdio MCP 서버
- 별도 영구 프로필의 Google Chrome 관리 런타임
- 공식 교육과정 우선 추천기
- 버전이 있는 `ActivitySpec`
- 분모가 다른 분수 비교 템플릿
- MathCanvas 네이티브 객체 컴파일러
- fail-closed validator
- macOS·Windows 설치와 한국어 온보딩

Chrome 추가 기능, 개발자 모드 설치, 연결 코드, loopback HTTP 서버와 고정 포트는 제거했습니다. 삭제한 이전 빌드 폴더는 로컬 휴지통 `/Users/yubyeongju/.Trash/mathcanvas-obsolete.r1QWNW`에서 복구할 수 있습니다.

## 현재 자동 검증

- TypeScript typecheck: 통과
- Vitest: 15개 파일, 74개 테스트 통과
- 전체 workspace production build: 통과
- 실제 설치된 Google Chrome 150으로 전용 임시 프로필 실행: 통과
- 실제 관리형 런타임으로 `https://mathcanvas.vivasam.com/ko/myCanvas` 열기: 통과
- 공개 기준 프로젝트로 분모 1~12·기본 객체 전체 계약 검사: 통과
- 비로그인 상태를 `login-required`로 안전하게 판정: 통과
- 의존성 감사: 알려진 취약점 없음
- macOS 설치 스크립트 zsh 구문: 통과
- security-audit 순차 검토와 결과 스키마 검증: 발견된 악용 가능 취약점 0건

## 교육 QA

- 공식 `[6수01-07]` 목표와 5~6학년군 확인
- 교육부 원문 PDF 31쪽과 DECK6 고정 커밋을 각각 확인
- 같은 전체 폭과 같은 출발선 강제
- 분수 관계를 교차곱으로 검증
- 분모가 서로 다르고 크기가 같은 쌍은 제외
- 쉬움 28~56%, 보통 15~27%, 어려움 8~14.5%로 난이도 순서 고정
- 순서만 바꾼 같은 분수 비교와 겹치는 지시문 영역 차단
- 학생에게 비교 이유를 말하는 단계 제공
- 추천·생성 응답에 비교 방법이 담긴 교사용 정답지 포함
- 로그인 실패 뒤 같은 추천안·같은 문제로 재시도하는 회귀 테스트 통과
- MathCanvas 네이티브 좌표를 출발선에 옮겼을 때 실제 왼쪽 끝과 폭이 맞는지 검증

이 활동은 시각적 개념 형성용입니다. `[6수01-07]` 전체 도달 여부를 평가하려면 학생이 비교 방법을 말하거나 쓰는 후속 확인이 필요합니다.

## 아직 실서비스에서 확인하지 않은 항목

전용 임시 프로필에는 사용자 계정 로그인이 없으므로 실제 새 프로젝트 쓰기는 실행하지 않았습니다.

- 실제 `projectId` 발급과 편집 화면 렌더링
- 분수 띠와 `<`·`>` 기호의 실제 드래그
- 실제 폰트 렌더링에서 지시문·비교판 겹침 여부
- Windows 실기기 설치

이 항목은 교사가 전용 Chrome에서 로그인한 뒤 제작자 계정 새 프로젝트 한 건으로 확인합니다.
