# MathCanvas 메뉴·학생 조작 감사

감사일: 2026-07-29
대상 캔버스: `feeTES` 2/4
브라우저: Chrome `1710×895`, DPR 2
수행 주체: Codex 단독

## 현재 판정

기존 v2.3.0 활동지는 **FAIL**이다. 이전 PASS와 점수는 철회한다.

실패 이유는 다음 세 가지다.

1. 학생용 글쓰기 객체가 있지만, 첫 클릭 전에는 입력칸인지 알 수 없다.
2. 드래그 띠는 네이티브 분수 모형을 썼지만, 분수 카드 표시는 수식 입력기의
   분수 템플릿 대신 분자·가로선·분모를 여러 객체로 조립했다.
3. 넓은 브라우저에서도 기본 60% 보기 상태의 활동판 폭이 약 669px뿐인데,
   이 실제 학생 화면 크기에서 사용성을 판단하지 않았다.

## v2.4.1 보완 결과

2026-07-29에 아래 계약으로 새 캔버스를 다시 만들었다.

- 읽는 분수 4개는 각각 `math-latex`의 `\frac{분자}{분모}` 한 객체다.
- 실제 `math-field` 기준 분수·비교 기호의 가로·세로 중심 오차를
  1px 이하로 강제하며, 최종 최대 오차는 0.20px이다.
- 기존 `분자 + 선 + 분모` 조립 객체는 0개이며 validator가 재도입을
  차단한다.
- 조작 띠는 기존대로 `NO03FM` 네이티브 분수 모형이다.
- `3. 더 긴 띠와 까닭을 써요 →` 오른쪽에 노란 빈 입력칸을 분리했다.
- 최종 기본 배율은 `scale=2`, 화면 표시 120%다.
- `1710×895`, DPR 2에서 비교판 폭은 1180px이며, 응답 패널과 하단
  도구막대 사이 실제 간격은 15.5px다.

최신 최종 URL과 화면 근거는 `REPORT.md`와
`qa/live-remediation-2026-07-29/final-v241-centered/`에 둔다. 이 문서의
v2.3.0 실패 분석은 회귀 방지 기록으로 계속 보존한다.

## 상단 메뉴

| 메뉴 | 확인한 동작 | 제작 규칙 |
|---|---|---|
| 미리보기 | 같은 탭에서 학생 조작 상태로 전환 | 반드시 첫 클릭 전 상태까지 검사한다. |
| URL복사 | 현재 캔버스 URL 복사 | 교사 전달 단계에서만 사용한다. |
| 나가기 | 편집 화면 종료 | 제작·QA 중에는 누르지 않는다. |

## 왼쪽 도구 목록

### 수와 연산 → 분수 모형

분수 띠와 원 조각을 각각 `1`, `1/2`부터 `1/12`까지 제공한다.

- 분수 띠: `NO03FM-10`부터 `NO03FM-01`, `NO03FM-21`,
  `NO03FM-22`
- 원 조각: `NO03FM-20`부터 `NO03FM-11`, `NO03FM-23`,
  `NO03FM-24`
- 띠·원마다 `모두 꺼내기`가 따로 있다.

도움말에서 확인한 네이티브 동작은 다음과 같다.

1. 조절점을 움직여 분수 모형의 크기를 늘이거나 줄인다.
2. 분수를 단위분수 크기로 자른다.
3. 분수 표시를 켜거나 끈다.

왼쪽 패널을 접으면 시야는 정리되지만 활동판 자체 크기는 커지지 않는다.
`120%`에서 패널을 접기 전후 활동판 폭은 모두 약 `1156px`이었다.

## 아래 공통 도구

| 순서 | 메뉴 | 단축키 | 실제 확인 |
|---:|---|---|---|
| 1 | 실행 취소 | - | 객체 편집 이력 되돌리기 |
| 2 | 다시 실행 | - | 되돌린 편집 다시 적용 |
| 3 | 선택 | `V` | 객체 선택·이동·편집 |
| 4 | 펜 | `P` | 색상 팔레트가 열림 |
| 5 | 지우개 | `E` | 지우개 모드 선택 |
| 6 | 점 / 선 | `L` | 점·선 그리기 모드 선택 |
| 7 | 사각형 | `R` | 사각형 그리기 모드 선택 |
| 8 | 원 | `O` | 원 그리기 모드 선택 |
| 9 | 텍스트 | `T` | **누르는 즉시 중앙에 새 텍스트 객체 생성** |
| 10 | 수식 | - | 수식 입력기 모달 열기 |

`텍스트`는 일반적인 “도구만 선택” 동작이 아니다. 최종 캔버스에서 메뉴를
시험 삼아 누르면 새 객체가 생긴다. 메뉴 QA는 전용 새 캔버스에서 해야 한다.

### 수식 입력기

수식 입력기는 입력 영역, `입력하기` 버튼, 아래 12개 범주로 구성된다.

| 범주 | 대표 템플릿 |
|---|---|
| 기호 | `+ − × ÷ = ≠ ± < > ≤ ≥ π α β γ ∞ → ←` |
| 분수, 괄호 | 진분수, 대분수, 소괄호·중괄호·대괄호·절댓값 |
| 첨자 | 제곱·세제곱·윗첨자·아랫첨자 |
| 제곱근 | 제곱근·세제곱근·n제곱근 |
| 합계 | 시그마와 수열 합 |
| 함수 | 일차·이차함수, 합성함수, 역함수 |
| 삼각비 | `sin`, `cos`, `tan`, 각도 |
| 행렬 | 2×2, 3×3, 행·열·일반 행렬 |
| 순환소수 | 0~9 위 점 표시 |
| 기하 | 선분·직선·반직선·각·평행·수직·합동·닮음 |
| 단위 | 길이·넓이·부피·무게·들이·온도·속력 |
| 집합 | 원소·부분집합·합집합·교집합 |

현재 드래그 띠 `problem-2-left-strip`, `problem-2-right-strip`은
`NO03FM` 네이티브 분수 모형이다. 반면 화면의 `5/6`, `3/5` 카드 표시는
분자·가로선·분모를 독립 객체로 조립했다.

새 활동지부터 조작 모형은 왼쪽의 네이티브 분수 모형을, 읽는 분수식은
수식 입력기의 진분수·대분수 템플릿을 우선한다. 분자·가로선·분모를
독립 텍스트와 선으로 조립하는 방식은 예외 사유가 없으면 금지한다.

## 오른쪽 보기 도구

| 메뉴 | 확인한 동작 |
|---|---|
| 새로고침 | 툴팁만 확인. 최종본 상태 변경 위험 때문에 실행하지 않음 |
| 전체 화면 | 아이콘은 `전체 화면 종료`로 바뀌지만 현재 환경에서 활동판 크기는 그대로 |
| 그리드 | 없음 포함 9개 격자·점·삼각 격자 선택 팔레트 |
| 확대 | `60 → 80 → 100 → 120%` |
| 축소 | 확대 단계의 역순 |
| 이동 | `SPACE`, 캔버스 시점 이동 |

## 화면 배율 감사

브라우저 `1710×895`에서 `#playground-2`의 실제 렌더 폭을 측정했다.

| 표시 배율 | 활동판 실제 폭 | 브라우저 폭 사용률 |
|---:|---:|---:|
| 60% | 약 669px | 39.1% |
| 80% | 약 803px | 47.0% |
| 100% | 약 963px | 56.3% |
| 120% | 약 1156px | 67.6% |

왼쪽 도구 영역을 제외한 사용 가능 폭을 기준으로 보면 120%에서 약 78%를
쓴다. 전체 화면과 왼쪽 패널 접기는 현재 활동판 배율을 자동으로 높이지
않았다.

따라서 화면이 좁은 직접 원인은 MathCanvas의 기본 60% 보기 상태다.
제작 측 실패는 이 상태를 실제 학생 화면으로 보고도 확대·가독성·조작
QA를 하지 않은 것이다.

## 학생 글쓰기 동작

`problem-2-explanation-input`은 미리보기에서 다음처럼 동작한다.

- 첫 클릭 전: 일반 문장처럼 보이며 `contenteditable="false"`
- 클릭 후: 객체가 활성화되고 내부가 `contenteditable="true"`로 바뀜
- 보이는 문장 `어느 띠가 더 긴지 한 줄로 써요.`는 별도 placeholder가
  아니라 실제 객체 내용
- 일반 지시문과 분수 숫자는 클릭해도 편집 상태가 되지 않음

즉, 기술적으로 입력은 가능하지만 학생이 어디를 눌러야 하는지 알 수 있는
빈칸, 연필 아이콘, `여기에 쓰기` 라벨, 배경 대비가 없다. 현재 화면은
학생 입력 UI로 인정하지 않는다.

## 다음 활동지 제작 강제 규칙

1. 메뉴 실험은 항상 전용 새 캔버스에서 한다.
2. 조작 띠는 네이티브 분수 모형을, 분수식은 수식 입력기를 먼저 사용한다.
3. 학생 입력 대상은 지시문과 분리된 빈 작업 영역으로 보이게 한다.
4. 미리보기 첫 클릭 전 3초 보기에서 학생이 입력 위치를 가리키지 못하면
   실패다.
5. 기본 60%와 권장 120%를 모두 캡처하고 실제 rect를 기록한다.
6. 전체 화면·패널 접기를 “자동 확대”로 간주하지 않는다.
7. 사용자가 발견한 두 화면을 영구 회귀 테스트로 유지한다.
8. 새 활동지를 만들기 전에 전용 QA 캔버스에서 네이티브 분수 객체의
   생성·크기 조절·자르기·표시 전환·학생 드래그를 끝까지 검증한다.
