# 교육 설계와 문구 QA

## 교육과정 기준

- 공식 성취기준: `[6수01-07] 분모가 다른 분수의 크기를 비교하고 그 방법을 설명할 수 있다.`
- 학년군: 초등 5~6학년군
- 영역: 수와 연산
- 권장 학년: 5학년
- 보조 자료: `DECK6/korean-elementary-learning-map` 커밋 `3ef0563084aa2a9baaa47da2c1ec0ebf5d7edc5c`

교육부 원문 PDF 26쪽에서 코드와 목표를 대조했습니다. 보조 학습 맵의 고정 커밋은 같은 코드를 상위 단원 `분수의 덧셈과 뺄셈` 아래에 둡니다. 공식 목표는 교육부 원문을 기준으로 삼습니다.

## 첫 활동의 수학 행동

1. 학생이 띠를 옮기기 전에 `<`, `=`, `>` 중 하나를 예상해 씁니다.
2. 학생이 두 분수 띠를 같은 출발선으로 옮깁니다.
3. 같은 전체 길이에서 색칠된 길이를 비교합니다.
4. 세 기호 중 하나를 관계 칸에 놓습니다.
5. 예상과 다르면 선택을 고치고, 같은 전체와 띠의 길이를 근거로 기록합니다.

두 분수가 절대 같지 않다는 정보를 선택지 자체가 알려 주지 않도록 관계의 전체 집합을 제공합니다. 학생은 같은 전체와 같은 출발점이라는 비교 조건을 직접 만들고, 예측과 띠의 결과가 충돌하면 선택을 수정합니다.

두 띠의 길이 차이는 전체의 8% 이상이어야 합니다. 쉬움은 28~56%, 보통은 15~27%, 어려움은 8~14.5%로 구성합니다. 생성기와 생성 직전 validator가 각각 검사합니다. 어려워질수록 분모가 커지고 띠 길이 차이가 줄어듭니다.

교사가 추천안을 확인할 때 각 문제의 비교식, 통분식, 분수 띠 설명을 담은 정답지도 함께 보여 줍니다. 예를 들어 `5/6=25/30`, `3/5=18/30`을 띠 길이와 연결합니다. 이 템플릿은 분수 띠로 크기를 비교하는 개념 형성 활동입니다. 성취기준 전체를 확인하려면 학생이 비교 방법을 말하거나 쓰는 후속 확인이 필요합니다. MathCanvas가 제공하지 않는 자동 채점이나 오답 피드백을 이 도구가 있는 것처럼 만들지는 않습니다.

## 등록 활동과 variation

- 분수 크기 비교 (`released`): 5학년 권장, 2~6문제, 쉬움·보통·어려움, 혼합·서로소·배수 분모 관계
- 동치분수 (`released`): 5학년 권장, 2~6문제, 분수 띠 6개 중 예상·선택·같은 전체 비교·설명
- 10 만들기 (`released`): 2학년 권장, 2~5문제, 카드 6장 중 두 장을 예상·구성·10칸 확인·다른 방법과 까닭 기록
- 등호 양쪽의 값 맞추기 (`released`): 4학년 권장, 2~4문제, 답 카드 예상·선택·18칸 끝점 비교·생각 수정과 까닭 기록
- 접시저울로 합 찾기 (`released`): 4학년 권장, 2~4문제, 합을 예상하고 오개념을 반영한 카드 중 하나를 골라 실제 기울기로 확인한 뒤 설명·수정
- 짧은바늘 위치 판단 (`released`): 2학년 권장, 2~4문제, 짧은바늘의 위치를 예상하고 긴바늘을 시계가 가는 쪽으로 돌려 실제 연동을 확인한 뒤 설명·수정
- 걸린 시간 (`released`): 2학년 권장, 2~4문제, 시 경계를 지나는 걸린 시간을 예상하고 시작 시계를 끝 시각까지 돌려 1시간과 60분의 관계를 확인한 뒤 설명·수정
- 같은 분모 분수 덧셈 (`released`): 4학년 권장, 2~4문제, 분모까지 더하기 등 오개념 답을 먼저 고르고 두 분수 띠를 빈틈없이 이어 붙여 같은 단위 조각과 합을 확인한 뒤 설명·수정
- 1을 넘는 같은 분모 분수 덧셈 (`released`): 4학년 권장, 2~4문제, 분모까지 더하기·1에서 멈추기·넘은 부분만 읽기 오개념 답을 먼저 고르고 두 분수 띠가 1의 금을 넘는지 확인한 뒤 설명·수정
- 공통 단위로 더하는 분모가 다른 분수의 덧셈 (`released`): 5학년 권장, 2~3문제, 분자·분모를 각각 더하기·분모만 통분하기·큰 부분만 읽기·곱셈 혼동 답을 먼저 고르고 서로 다른 두 분수 띠를 이어 붙여 공통 단위 자의 칸 경계로 확인한 뒤 설명·수정
- 공통 단위로 빼는 분모가 다른 분수의 뺄셈 (`released`): 5학년 권장, 2~3문제, 한쪽만 통분하기·덮은 부분 읽기·빼지 않기·분모끼리 더하기 오개념 답을 먼저 고르고 빼는 띠를 오른쪽 끝에 맞춰 덮어 남은 공통 단위 칸으로 확인한 뒤 설명·수정
- 막대그래프 눈금 한 칸의 값 (`released`): 4학년 권장, 2~3문제, 기준 막대에서 눈금 한 칸의 값을 먼저 정하고 다른 막대의 값을 읽어 선택을 설명·수정
- 1 cm 단위 반복 길이 재기 (`released`): 2학년 권장, 2~3문제, 자의 눈금값과 실제 길이를 구별하고 1 cm 막대를 반복해 옮겨 선택을 설명·수정
- 십 모형 10개와 100칸의 자릿값 (`released`): 2학년 권장, 2~3문제, 개수 이어 쓰기·십 묶음 누락·십 모형을 일로 세기·자리 뒤바꾸기 오개념 수를 먼저 고르고 십 모형을 서로 다른 묶음판 10칸에 하나씩 놓아 10줄 100칸과 연결한 뒤 설명·수정
- 가장 짧은 반복 단위 찾기 (`released`): 2학년 권장, 2~3문제, 전체 조각 수나 같은 색의 수를 반복 단위로 보는 생각을 먼저 드러내고 패턴 블록으로 다음 두 무늬를 완성해 순서가 이어지는지 확인한 뒤 설명·수정
- 묶음 배열과 곱셈식 연결 (`released`): 2학년 권장, 2~3문제, 두 수의 순서 바꾸기·덧셈식·한 묶음 부족·같은 수 제곱 오답 중 하나를 고르고 괄호 속 점 수와 괄호 수를 세어 식의 두 수가 뜻하는 바를 설명·수정
- 두 주머니 가능성 비교 (`released`): 6학년 권장, 2~4문제, 빨강 공 수만 비교하거나 전체 공 수만 비교하는 생각을 먼저 드러내고 전체 공 수에 대한 빨강 공 수를 같은 전체 길이의 띠로 비교한 뒤 설명·수정

열일곱 활동의 variation은 구조·수학·인지적 요구·교실 언어·텍스트 배치를 함께 검증합니다. 10 만들기는 문항마다 정확한 해 두 쌍, 쓰이지 않는 카드 두 장, 합이 9 또는 11인 근접 오답을 보장합니다. 등호 활동은 정답·왼쪽 계산값·오른쪽의 보이는 수·1 차이 오답을 포함하고, 모든 카드를 18칸 안에서 직접 반증할 수 있게 합니다. 접시저울 활동은 정답·두 덧셈 수·차·1 차이 오답을 함께 제시하고, 카드 선택의 결과를 저울 기울기로 직접 반증하게 합니다. 짧은바늘 활동은 정각에만 움직이거나 긴바늘 숫자를 가리킨다는 오개념을, 걸린 시간 활동은 분끼리 빼기·60분 고정·100분 빌림 오개념을 기어식 시계 조작으로 직접 반증합니다. 같은 분모 분수 덧셈은 분모까지 더하기·큰 덧수 유지·맞닿은 경계 중복 세기·빼기 오개념을 같은 단위의 두 띠를 이어 붙이는 결과로 직접 반증합니다. 1을 넘는 합 활동은 분모까지 더하기·1에서 멈추기·넘은 부분만 읽기·큰 덧수 유지 오개념을 1의 금을 지나는 두 띠의 길이로 직접 반증합니다. 분모가 다른 분수의 덧셈은 분자와 분모를 각각 더하기·분모만 통분하기·큰 부분만 읽기·곱셈 혼동 오개념을 같은 크기의 칸으로 나눈 자와 두 띠의 끝점으로 직접 반증합니다. 분모가 다른 분수의 뺄셈은 한쪽만 통분하기·덮은 부분 읽기·빼지 않기·분모끼리 더하기 오개념을, 빼는 띠를 오른쪽 끝에 맞춰 덮고 남은 공통 단위 칸을 읽는 결과로 직접 반증합니다. 막대그래프 활동은 기준 막대로 눈금 한 칸의 값을 정한 뒤 다른 막대의 값을 읽게 하고, 길이 활동은 자의 시작점과 어긋나 놓인 연필을 따라 1 cm 막대를 반복해 옮겨 선택을 고치게 합니다. 자릿값 활동은 십 모형 10개를 서로 다른 열 칸에 놓고 10줄 100칸과 연결해 처음 고른 수와 설명을 고치게 합니다. 반복 규칙은 가장 짧은 반복 단위로 다음 두 무늬가 모두 이어지는지 확인하고, 곱셈은 한 묶음의 수와 묶음 수를 배열에서 각각 확인하며, 가능성은 빨강 공 수만이 아니라 전체 공에 대한 비율을 같은 전체 길이의 띠로 비교합니다. 현재 열일곱 활동은 새 화면 canary와 출시 증거 검사를 통과했습니다. 다른 수학 주제는 아직 등록된 blueprint가 없으므로 생성하지 않습니다.

## Humanizer 학생 문구 QA

검사 대상:

- `분수 띠를 같은 출발선에 놓아요.`
- `두 띠의 길이를 보고 알맞은 기호 하나를 고르세요.`
- `예상과 달랐다면 고친 뒤, 같은 전체와 길이를 근거로 설명하세요.`
- `첫째 띠`
- `둘째 띠`
- `기호 놓는 곳`
- `같은 전체에서 두 띠를 같은 출발선에 놓으면 5/6 띠가 더 깁니다.`
- `더 큰 분수는 5/6입니다.`

판정:

- 한 문장에 행동 하나
- 초등학생이 소리 내어 읽기 쉬움
- `메커니즘`, `오브젝트`, `게이트`, `payload` 같은 제작자 말 없음
- 번역투와 과도한 명사 나열 없음
- 같은 뜻 반복 없음

결과: 통과

Humanizer 자연도: A. 문구가 짧아 통계적 패턴 분석에는 한계가 있지만, 번역투·어른 중심 한자어·불필요한 쉼표·명사 나열이 없습니다. 수치·고유명사·수학적 관계와 학생 행동을 그대로 보존했습니다.

## 아직 검증하지 않은 범위

- MathCanvas 실제 학생 활동 모드의 드롭 판정과 자동 피드백
- 교실 학생 대상 사용성 관찰
- 5학년 교사의 실제 배포 전 내용 검토

이 범위는 MathCanvas 편집기 실제 생성본을 만든 뒤 별도 파일럿으로 확인합니다.

## P3 실제 생성 canary

2026-07-31T06:02:40.471Z에 현재 화면의 세 활동을 headless로 각각 새 프로젝트 1회 생성했습니다. `research/mathcanvas/p3-release-canary.json`은 3/3 성공, 활동별 POST 1회, 기존 프로젝트 쓰기 0회와 현재 blueprint·layout hash 결속을 기록합니다.

이전 10 만들기 2.0.0의 수식 레일 광학 검토는 `research/mathcanvas/w3-equation-rail-optical.json`에 보관합니다. 현재 2.1.0 출시는 별도 과거 증거가 아니라 최신 P3 canary의 blueprint·layout hash 결속으로 검증했습니다.
